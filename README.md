# Delay Multi-Channel Relay Controller

🔌 โมดูลควบคุม Relay 4 ช่อง สำหรับ MicroPython 1.6.0+ พร้อมฟังก์ชันหน่วงเวลาและการใช้งานที่ง่ายดาย

## ✨ คุณสมบัติหลัก

- **4 ช่อง Relay** - ควบคุมโดยใช้ GPIO 15, 18, 19, 21
- **Active Low Logic** - เปิด relay ส่ง 0, ปิด relay ส่ง 1 
- **ฟังก์ชันรอ/หน่วงเวลา** - แยกเป็นฟังก์ชันสำหรับใช้งานง่าย
- **เปิด/ปิดทั้งหมด** - ควบคุมทุกช่องพร้อมกัน
- **การแสดงสถานะ** - ตรวจสอบสถานะปัจจุบันของแต่ละช่อง
- **ฟังก์ชันขั้นสูง** - สลับสถานะ, เปิด/ปิดตามลำดับ

## 📌 การเชื่อมต่อ GPIO

| ช่อง Relay | GPIO Pin | คำอธิบาย |
|-----------|----------|----------|
| ช่อง 1    | GPIO 15  | Relay Channel 1 |
| ช่อง 2    | GPIO 18  | Relay Channel 2 |
| ช่อง 3    | GPIO 19  | Relay Channel 3 |
| ช่อง 4    | GPIO 21  | Relay Channel 4 |

## 🚀 การติดตั้งและใช้งาน

### 1. Import โมดูล
```python
import modules.relay_4ch as relay
```

### 2. การใช้งานพื้นฐาน
```python
# เปิด/ปิดขาเดี่ยว
relay.on(1)         # เปิด relay ช่อง 1
relay.off(1)        # ปิด relay ช่อง 1

# เปิด/ปิดทั้งหมด
relay.on_all()      # เปิด relay ทุกช่อง
relay.off_all()     # ปิด relay ทุกช่อง

# ฟังก์ชันรอ/หน่วงเวลา
relay.wait(2)       # รอ 2 วินาที
```

### 3. การใช้งาน Blockly (Block Programming)
โปรเจคนี้รองรับการเขียนโปรแกรมแบบ Block ผ่าน Blockly:

**บล็อกที่รองรับ:**
- **เปิดรีเลย์** - เปิด relay ช่องที่เลือก (1-4)
- **ปิดรีเลย์** - ปิด relay ช่องที่เลือก (1-4) 
- **เปิดรีเลย์ทั้งหมด** - เปิด relay ทุกช่องพร้อมกัน
- **ปิดรีเลย์ทั้งหมด** - ปิด relay ทุกช่องพร้อมกัน
- **รอ** - หน่วงเวลาตามจำนวนวินาทีที่กำหนด

### 4. การใช้งานขั้นสูง
```python
# สลับสถานะ
relay.toggle(1)     # สลับสถานะ relay ช่อง 1

# ดูสถานะปัจจุบัน
relay.status()      # แสดงสถานะทุกช่อง

# เปิด/ปิดตามลำดับ
relay.sequence_on(0.5)    # เปิดทีละช่อง หน่วง 0.5 วินาที
relay.sequence_off(0.5)   # ปิดทีละช่อง หน่วง 0.5 วินาที
```

## 📚 รายการฟังก์ชันทั้งหมด

### ฟังก์ชันพื้นฐาน
| ฟังก์ชัน | พารามิเตอร์ | คำอธิบาย |
|---------|-------------|----------|
| `on(ch)` | ch: ช่อง 1-4 | เปิด relay ช่องที่กำหนด |
| `off(ch)` | ch: ช่อง 1-4 | ปิด relay ช่องที่กำหนด |
| `on_all()` | - | เปิด relay ทุกช่อง |
| `off_all()` | - | ปิด relay ทุกช่อง |
| `wait(seconds)` | seconds: วินาที | หน่วงเวลาตามที่กำหนด |

### ฟังก์ชันเพิ่มเติม
| ฟังก์ชัน | พารามิเตอร์ | คำอธิบาย |
|---------|-------------|----------|
| `open_all()` | - | เปิดทั้งหมด (alias ของ on_all) |
| `close_all()` | - | ปิดทั้งหมด (alias ของ off_all) |
| `toggle(ch)` | ch: ช่อง 1-4 | สลับสถานะ relay |
| `status()` | - | แสดงสถานะปัจจุบันทุกช่อง |

### ฟังก์ชันขั้นสูง
| ฟังก์ชัน | พารามิเตอร์ | คำอธิบาย |
|---------|-------------|----------|
| `sequence_on(delay)` | delay: วินาที (default=1) | เปิดทีละช่องตามลำดับ |
| `sequence_off(delay)` | delay: วินาที (default=1) | ปิดทีละช่องตามลำดับ |

## 💡 ตัวอย่างการใช้งานจริง

### ตัวอย่าง 1: ระบบไฟกระพริบ
```python
import modules.relay_4ch as relay

# กระพริบไฟทีละช่อง
def blink_lights():
    for i in range(3):  # กระพริบ 3 รอบ
        relay.sequence_on(0.3)     # เปิดทีละช่อง เร็วๆ
        relay.wait(1)              # เปิดค้าง 1 วินาที
        relay.sequence_off(0.3)    # ปิดทีละช่อง เร็วๆ
        relay.wait(0.5)            # พัก 0.5 วินาที

blink_lights()
```

### ตัวอย่าง 2: ระบบรดน้ำต้นไม้
```python
import modules.relay_4ch as relay

def water_plants():
    zones = [1, 2, 3, 4]  # โซนรดน้ำ
    
    for zone in zones:
        print(f"เริ่มรดน้ำโซน {zone}")
        relay.on(zone)        # เปิดปั๊มน้ำ
        relay.wait(30)        # รดน้ำ 30 วินาที
        relay.off(zone)       # ปิดปั๊มน้ำ
        relay.wait(5)         # พักระหว่างโซน 5 วินาที
    
    print("รดน้ำเสร็จสิ้น")

water_plants()
```

### ตัวอย่าง 3: ควบคุมอุปกรณ์บ้าน
```python
import modules.relay_4ch as relay

# เปิดอุปกรณ์ทั้งหมดตอนกลับบ้าน
def arrive_home():
    print("กลับมาบ้านแล้ว - เปิดอุปกรณ์")
    relay.on(1)           # เปิดไฟหลักบ้าน
    relay.wait(1)
    relay.on(2)           # เปิดพัดลม
    relay.wait(1)
    relay.on(3)           # เปิดเครื่องกรองอากาศ
    relay.status()        # ตรวจสอบสถานะ

# ปิดอุปกรณ์ทั้งหมดก่อนออกจากบ้าน
def leave_home():
    print("ออกจากบ้าน - ปิดอุปกรณ์ทั้งหมด")
    relay.off_all()       # ปิดทุกอย่างพร้อมกัน
    relay.status()        # ตรวจสอบให้แน่ใจ

# arrive_home()
# leave_home()
```

## ⚡ หลักการทำงาน

**Active Low Logic:**
- ส่งสัญญาณ `0` (LOW) = เปิด relay 
- ส่งสัญญาณ `1` (HIGH) = ปิด relay

**การเชื่อมต่อ:**
- GPIO pins เชื่อมต่อกับขา control ของ relay module
- Relay module ต้องมีการป้อนไฟแยกสำหรับขับ coil
- Common, NO (Normally Open), NC (Normally Closed) เชื่อมต่ออุปกรณ์ที่ต้องการควบคุม

## 🔧 การแก้ปัญหา

### ปัญหาทั่วไป
1. **Relay ไม่ทำงาน** - ตรวจสอบการเชื่อมต่อและไฟเลี้ยง
2. **GPIO ไม่ตรงกัน** - ตรวจสอบพิน GPIO ให้ตรงกับฮาร์ดแวร์
3. **Logic กลับกัน** - โมดูลนี้ใช้ Active Low (0=เปิด, 1=ปิด)

### การตรวจสอบ
```python
# ตรวจสอบสถานะปัจจุบัน
relay.status()

# ทดสอบทีละช่อง
for ch in range(1, 5):
    relay.on(ch)
    relay.wait(1)
    relay.off(ch)
    relay.wait(0.5)
```

## 📁 โครงสร้างโปรเจค

```
delay-multi-channel/
├── modules/
│   └── relay_4ch.py      # โมดูลหลักสำหรับควบคุม relay
├── blocks/               # ส่วนขยายสำหรับ block programming
├── static/               # ไฟล์ static
├── extension.js          # ส่วนขยาย
└── README.md            # ไฟล์นี้
```

## ⚠️ ความเข้ากันได้

**MicroPython Version:** 1.6.0+
- โค้ดถูกออกแบบให้รองรับ MicroPython 1.6.0 และเวอร์ชันที่ใหม่กว่า
- ใช้ string formatting แบบเก่าเพื่อความเข้ากันได้
- ไม่ใช้ f-strings หรือฟีเจอร์ที่ใหม่เกินไป

**ฮาร์ดแวร์ที่รองรับ:**
- ESP32 / ESP8266
- บอร์ดอื่นๆ ที่รัน MicroPython (อาจต้องปรับ GPIO pins)

## 📄 License

โปรเจคนี้เป็น Open Source สามารถนำไปใช้และพัฒนาต่อได้อย่างอิสระ

---

**หมายเหตุ:** โมดูลนี้ถูกออกแบบสำหรับ MicroPython 1.6.0+ บน ESP32/ESP8266 หากใช้กับบอร์ดอื่น อาจจำเป็นต้องปรับ GPIO pins ให้เหมาะสม 